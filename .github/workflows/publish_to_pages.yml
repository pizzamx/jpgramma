name: Deploy To GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@releases/v4.1.1
        with:
          repository_name: pizzamx/pizzamx.github.io
          branch: master
          folder: jpgramma